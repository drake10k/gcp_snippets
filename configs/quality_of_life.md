### Disable accessibility/screen_reader (prints output in table format):

```bash
gcloud config set accessibility/screen_reader false
```

### Set default compute zone:

```bash
gcloud config set compute/zone <your_zone>
```